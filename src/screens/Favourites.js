import React from "react";
import { StyleSheet, Text, View } from "react-native";

const Favourites = () => {
  return (
    <View>
      <Text>Favourite Products</Text>
    </View>
  );
};

export default Favourites;

const styles = StyleSheet.create({});
